import SwiftUI

extension Color {
    enum DS {
        static let portalGreen    = Color(red: 0.17, green: 0.76, blue: 0.29)
        static let portalGreenDim = Color(red: 0.12, green: 0.55, blue: 0.20)
        static let statusAlive    = Color(red: 0.18, green: 0.80, blue: 0.44)
        static let statusDead     = Color(red: 0.91, green: 0.30, blue: 0.24)
        static let statusUnknown  = Color(red: 0.60, green: 0.60, blue: 0.60)
        static let cardBackground = Color(.secondarySystemBackground)
        static let surfacePrimary = Color(.systemBackground)
        static let textPrimary    = Color(.label)
        static let textSecondary  = Color(.secondaryLabel)
        static let textTertiary   = Color(.tertiaryLabel)
        static let separator      = Color(.separator)
    }
}
